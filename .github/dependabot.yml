version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  # 注: web は bun を使用。Dependabot は bun.lock を更新できないため npm エコシステムは登録しない
  #     （frozen-lockfile で必ず失敗するため）。依存更新は `bun update` で手動運用する。
